import styles from '../styles/Home.module.css'

export default function PickCard () {
    return (
    <a href="conditional_link_to_api_to_claim" className={styles.card}>
      <p>Conditionally blank, owned tickets, or claim button.</p>
    </a>
  )
}